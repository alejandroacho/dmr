# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  Blackwell Smart Gateway — Python Dependencies
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Async web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Data models
pydantic>=2.9.0

# Async HTTP client (proxy to backends)
aiohttp>=3.10.0

# Docker SDK (container management — used when ORCHESTRATION_BACKEND=docker)
docker>=7.1.0

# Kubernetes client (used when ORCHESTRATION_BACKEND=kubernetes)
kubernetes>=28.1.0

# ASGI server
httptools>=0.6.0
websockets>=13.0

# GPU monitoring (NVML bindings — works inside containers without nvidia-smi binary)
nvidia-ml-py>=12.0.0

# Utilities
python-multipart>=0.0.12
orjson>=3.10.0
