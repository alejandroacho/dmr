apiVersion: v1
kind: Service
metadata:
  name: blackwell-gateway
  namespace: blackwell
spec:
  type: NodePort
  selector:
    app: blackwell-gateway
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
